// src/components/landing/hero.tsx
// Wrapper around the existing HeroSection for consistency with landing folder.

import { HeroSection } from '@/components/landing/HeroSection';

export function LandingHero() {
  return <HeroSection />;
}
