# FinPilot AI - Middleware Package
# ====================================
# This package contains custom middleware for request processing.
# Middleware runs on EVERY request before it reaches the route handler,
# and again on EVERY response before it's sent back to the client.
